// =============================================================================
// Phase 21 — account provisioning + forced first-login password change. Real
// browser -> vite preview -> DerasaX-backend -> PostgreSQL. No network mocking.
// Extends the Phase 8 helpers (login/nav/guards/reset).
// =============================================================================
export { BACKEND, PASSWORD, RESET_KEY, HAS_CREDS, CODES, backendUp, resetE2E, apiLogin, login, nav, setLang, attachGuards, axeSeriousOrCritical } from './ph8.js'
