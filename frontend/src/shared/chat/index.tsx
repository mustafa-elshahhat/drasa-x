// =============================================================================
// shared/chat — chat/tutor message UI (bubbles, AI-tutor message, thread).
// Implementations live in src/components/chat; this is the single import surface
// for pages/features.
// =============================================================================
export { ChatBubble, AITutorMessage, MessageThread } from '../../components/chat'
